import { styled } from "@mui/material/styles"
import { Typography } from "@mui/material"

export const StyledPageDescText = styled(Typography)(() => ({
  maxWidth: "100%",
  wordWrap: "break-word",
  color: "#000000",
}))
